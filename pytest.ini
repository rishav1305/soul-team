[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: End-to-end tests requiring live courier daemon (run with -m e2e)
    integration: Integration tests requiring real SSH/network connectivity (run with -m integration)
